#   # is for heading
#   installation
you can take the code without the last line for exports for CDN

`npm i npmtest --save`

Then...

```
import {npmtest} from 'npmtest';
npmtest({
    shadow_type:'soft',
    padding:false

});
```